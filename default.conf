server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
        # active development: don't let browsers cache the app shell
        add_header Cache-Control "no-cache, must-revalidate";
    }
}
